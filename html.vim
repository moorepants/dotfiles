set softtabstop=2 " sets the tab for size for indentation, will be a combination of tabs and spaces if expandtab isn't set
set shiftwidth=2 " autoindent and >> make 2 spaces
set textwidth=79 " wrap lines after 79 characters
set expandtab " the tab key in insert mode will output spaces instead of tabs
